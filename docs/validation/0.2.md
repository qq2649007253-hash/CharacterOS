# 0.2 验证记录

验证日期：2026-09-09，Windows，Node.js 24.19.0、pnpm 11.19.0。

| 检查 | 结果 |
| --- | --- |
| 依赖边界 | Web 无数据库或 API 业务导入；API 无 Next.js 导入 |
| TypeScript / ESLint | 通过 |
| 独立 API HTTP | 15 项断言通过，临时数据库隔离；涵盖动态路由、角色 CRUD、会话、无效输入、404/405、跨站 Origin、语音校验 |
| Web 生产构建 | 通过，业务路由集中在独立 API，Web 仅一个同源转发入口 |
| 原创角色初始化 | 新增苏晚、许知遥、夏栀，已有角色和对话未删除 |
| 浏览器聊天 | 苏晚接收消息并返回完整回复，历史标题和消息正常显示 |
| Windows 桌面包 | electron-builder 打包通过；可执行文件 smoke-test 退出码 0，确认 API、Web 与沙箱窗口加载 |
| 依赖复现 | pnpm install --frozen-lockfile 通过 |

## 验证边界

CI 配置已加入，记录的是本机验证结果，远程 CI 需推送后执行。
